using UnityEngine;

public class CellTable : MonoBehaviour
{
    public RectTransform stampRoot;

    // Start is called before the first frame update
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
